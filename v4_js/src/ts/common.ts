export type PoolEntry = {
  session: string,
  name: string,
  gameRequested: boolean
};

export type GameDetails = {
  action: string,
  name: string,
  specification: string,
  opponent: string,
  south: string
};
